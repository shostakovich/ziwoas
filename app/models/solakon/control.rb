module Solakon
  # Following the household with the inverter's output: the policy that decides a
  # target, the state it remembers between ticks, and the tick that applies it.
  module Control
    def self.table_name_prefix = ""
  end
end
