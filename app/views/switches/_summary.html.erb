<%# A summary has to be the first child of its details, so the count sits outside
    the rules container and needs a Turbo target of its own. %>
<summary id="sw_count_<%= row.plug.id %>">Schaltzeiten<%= " (#{row.rule_count})" if row.rule_count.positive? %></summary>
