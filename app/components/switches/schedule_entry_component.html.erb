<div class="sw-entry" id="<%= row_id %>">
  <%# Only an Einzelschaltung carries an arrow: a Zeitfenster says both
      directions by having two times. %>
  <span class="<%= pill_class %>">
    <%= helpers.entry_label(entry) %>
    <% unless window? %><span class="sw-dir"><%= direction %></span><% end %>
  </span>
  <%= button_to entry.enabled? ? "⏸" : "▶", enabled_path,
                method: :patch, params: { enabled: (!entry.enabled?).to_s },
                class: "sw-icon-btn", form_class: "sw-inline-form",
                "aria-label": "#{noun} #{entry.enabled? ? 'pausieren' : 'aktivieren'}" %>
  <%= link_to "✏️", edit_path, class: "sw-icon-btn", data: { turbo_stream: true },
              "aria-label": "#{noun} bearbeiten" %>
  <%= button_to "🗑", entry_path,
                method: :delete, class: "sw-icon-btn", form_class: "sw-inline-form",
                form: { data: { turbo_confirm: "#{noun} wirklich löschen?" } },
                "aria-label": "#{noun} löschen" %>
</div>
